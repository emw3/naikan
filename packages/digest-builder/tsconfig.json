{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Pure aggregation logic — only the `Date` global is used, so no Bun or Node
    // types are needed. Stays runtime-agnostic per ADR-0005 (imported by the
    // Node worker), like the other kernel packages (incident-machine, scheduler).
    "lib": ["ESNext"],
    "types": []
  },
  "include": ["src"]
}
